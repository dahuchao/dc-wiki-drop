/**
 * Création du controleur du wiki
 */
module.exports = function($state, $rootScope, $scope, $location, $resource, IdentificationService) {
  // Journalisation
  console.log('* dcWikiRedirect sur wiki.');
  // Changement d'état pour ouvrir le wiki
  //$state.go('wiki');
}
